<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mama">

    <!-- ============================================== -->
    <!--                 PERMISSIONS                    -->
    <!-- ============================================== -->

    <!-- 1. INTERNET (Pour l'API Urgences & Chatbot) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 2. LOCALISATION (Pour trouver les cliniques) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- 3. NOTIFICATIONS & ALARMES (Pour les Rendez-vous) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 4. AUDIO & SERVICE (Pour le Sommeil/Sleep Tracking de ton collègue) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Nécessaire pour Android 14+ si utilisation micro en arrière-plan -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mama"
        tools:targetApi="31">

        <!-- ========================================== -->
        <!--           ACTIVITÉS PRINCIPALES            -->
        <!-- ========================================== -->

        <!-- LOGIN : Point de départ (LAUNCHER) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- DASHBOARD : L'écran d'accueil après connexion -->
        <activity android:name=".HomeDashboardActivity" />

        <!-- AUTHENTIFICATION -->
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ForgotPasswordActivity" />

        <!-- ========================================== -->
        <!--              TES FONCTIONNALITÉS           -->
        <!-- ========================================== -->
        <activity android:name=".EmergencyActivity" />
        <activity android:name=".AppointmentsActivity" />
        <activity android:name=".ChatActivity" />
        <!-- Ancienne activité (gardée par sécurité) -->
        <activity android:name=".MainActivity" />

        <!-- ========================================== -->
        <!--        FONCTIONNALITÉS DU COLLÈGUE         -->
        <!-- ========================================== -->

        <!-- Modules Santé & Symptômes -->
        <activity android:name=".HealthActivity" />
        <activity android:name=".HealthTrackerActivity" />
        <activity android:name=".SymptomsActivity" />
        <activity android:name=".NutritionActivity" />
        <activity android:name=".ActivityWeatherActivity" />

        <!-- Module Sommeil & Enregistrements (Audio) -->
        <activity android:name=".SleepTrackingActivity" />
        <activity android:name=".RecordingsActivity" />
        <activity android:name=".WeeklyInfoActivity" />

        <!-- ========================================== -->
        <!--           SERVICES & RECEIVERS             -->
        <!-- ========================================== -->

        <!-- Ton Récepteur pour les notifs de RDV -->
        <receiver android:name=".NotificationReceiver" />

        <!-- Le Service de ton collègue pour enregistrer le sommeil -->
        <!-- (Indispensable pour éviter le crash du sleep tracker) -->
        <service
            android:name=".SleepTrackingService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

    </application>

</manifest>